## 1. Validate captured screening behavior

- [x] 1.1 Review `lib/features/screening/ui/screening_workspace_page.dart` against the new `screening-workspace` spec and resolve any mismatch between the code and the documented behavior.
- [x] 1.2 Add or update tests that cover the screening workspace filter flow and the exclude-note cancel/save behavior.

## 2. Finalize spec adoption

- [x] 2.1 Run `flutter analyze` and relevant tests after any screening-workspace validation updates.
- [x] 2.2 Archive this change and sync `screening-workspace` into `openspec/specs` once the documented behavior is confirmed.
